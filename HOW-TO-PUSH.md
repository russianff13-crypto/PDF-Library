# 🎯 كيف ترفع المشروع الآن - 3 دقائق فقط!

## ✅ الخطوة 1: أنشئ Repository (مرة واحدة فقط)

1. اذهب إلى: **https://github.com/new**
2. املأ:
   - **Repository name:** `PDF-Library` (بالضبط هكذا)
   - **Description:** مكتبة PDF بنظام Electron
   - **Public** أو **Private** (اختر ما تريد)
   - ❌ **لا تختر** أي خيارات أخرى (README, .gitignore, license)
3. اضغط **"Create repository"**

✅ انتهيت؟ رائع! الآن انتقل للخطوة 2

---

## 🚀 الخطوة 2: شغّل السكريبت

### الطريقة 1: السكريبت السريع (الأسهل) ⭐

افتح PowerShell في مجلد المشروع وشغّل:

```powershell
.\QUICK-PUSH.ps1
```

سيطلب منك اسم المستخدم فقط، ثم يرفع تلقائياً!

---

### الطريقة 2: السكريبت المفصل

```powershell
.\PUSH-NOW.ps1
```

هذا السكريبت يعطيك تفاصيل أكثر أثناء الرفع.

---

### الطريقة 3: أوامر يدوية (إذا السكريبتات ما اشتغلت)

```powershell
# استبدل YOUR-USERNAME باسم المستخدم الخاص بك
# واستبدل YOUR-TOKEN بالـ Token الخاص بك
$username = "YOUR-USERNAME"
$token = "YOUR-TOKEN"

git remote remove origin
git remote add origin "https://$username`:$token@github.com/$username/PDF-Library.git"
git branch -M main
git push -u origin main
```

---

## ⚠️ الخطوة 3: احذف Token فوراً (مهم!)

بعد الرفع بنجاح:

1. اذهب إلى: **https://github.com/settings/tokens**
2. ابحث عن Token الحالي
3. اضغط **"Delete"** أو **"Revoke"**
4. أكد الحذف

**لماذا؟** لأن Token موجود الآن في السكريبتات والملفات، يجب حذفه لأمان الحساب.

---

## 🔄 للاستخدام المستقبلي

### أنشئ Token جديد:
1. https://github.com/settings/tokens
2. "Generate new token (classic)"
3. اختر: `repo` فقط
4. احفظ Token في مكان آمن

### رفع تعديلات جديدة:
```powershell
git add .
git commit -m "وصف التعديل"
git push origin main
```

سيطلب منك Token في أول مرة فقط، بعدها سيُحفظ تلقائياً!

---

## 🆘 إذا واجهت مشاكل

### المشكلة: "repository not found"
**الحل:** تأكد من إنشاء Repository على GitHub أولاً (الخطوة 1)

### المشكلة: "permission denied"
**الحل:** تأكد من اسم المستخدم صحيح 100%

### المشكلة: السكريبت لا يعمل
**الحل:** استخدم الطريقة 3 (الأوامر اليدوية)

---

## ✅ Checklist

```
[ ] أنشأت Repository على GitHub (اسمه: PDF-Library)
[ ] شغلت السكريبت QUICK-PUSH.ps1
[ ] أدخلت اسم المستخدم
[ ] نجح الرفع!
[ ] حذفت Token القديم من GitHub
[ ] أنشأت Token جديد للمستقبل
```

---

## 🎉 بعد النجاح

رابط مشروعك سيكون:
```
https://github.com/YOUR-USERNAME/PDF-Library
```

استبدل `YOUR-USERNAME` باسم المستخدم الحقيقي!

---

**⏱️ الوقت: 3-5 دقائق فقط!**

**🚀 ابدأ الآن!**
